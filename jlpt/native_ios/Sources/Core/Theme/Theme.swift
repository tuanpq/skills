import SwiftUI

extension Color {
    /// Matches the Android app's palette so the two clients feel like the same product.
    static let jlptRed = Color(red: 0.702, green: 0.149, blue: 0.118)
    static let jlptRedContainer = Color(red: 1.0, green: 0.855, blue: 0.831)
}
